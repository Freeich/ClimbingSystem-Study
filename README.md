# 学习并复刻Lyra项目
## 🎬 演示视频
点击下图查看完整演示视频（B站）👇  

[![Demo](README.assets/Climb.png)](https://www.bilibili.com/video/BV1hoY1zmEoc)

# 主要步骤

- 步骤大致如下
- ![image-20250808012734897](README.assets/image-20250808012734897.png)
- 胶囊体检测：为了检测是否可以攀爬。
- 眼部射线检测 ：为了检测是否到达边缘
- <img src="README.assets/image-20250808030649958.png" alt="image-20250808030649958" style="zoom: 67%;" />
- 可以攀爬的条件：not (胶囊体碰到 and 眼部射线碰到 and 没有在下落)
- 接下来设置进入攀爬状态之后的一系列设置
- 首先设置进入攀爬状态
  - ![image-20250809005231040](README.assets/image-20250809005231040.png)
- 出攀爬状态的操作
  - ![image-20250809005318744](README.assets/image-20250809005318744.png)
- 接下来设置攀爬状态的物理状态和移动
  - 首先是物理状态：要处理可攀爬的表面、攀爬速度、攀爬旋转、把移动Snap到表面上否则角色会滑落
- 计算让角色能始终面向墙体的法线的反方向的旋转：
  - UE5中默认物体的前向是X轴
  - 做法：如果现在是根运动支配那么不改变旋转；如果不是就创建一个让由X轴转向墙的法向量的反方向的旋转。
  - ![image-20250809225901399](README.assets/image-20250809225901399.png)
  - 原理：
  - ![image-20250809224621005](README.assets/image-20250809224621005.png)
- 其次是，把角色的移动始终贴到墙上，即每次都计算沿墙面法线的“纠正位移”，让角色不管往哪移动都加上一个向量，都把角色拉回到墙上。
- 然后，判断停止攀爬的条件：计算墙面法向量与世界向上向量的夹角，当小于一定角度的时候就停止攀爬。
- （废弃）现在重建动画系统，目前使用的是Blendspace（权宜之计），先把系统实现了再说，再去解决八向移动的问题。
  - 现在已经修改为绑定Update函数根据攀爬的方向输出Cycle动画。

- 现在检查是否到达地面，如果到达地面就退出攀爬状态。
  - 其实现在当胶囊体碰到地面的时候，会有地面的碰撞点的normal加入进来，这个时候意外的触发了小于一定角度退出攀爬的条件，所以也会自动退出攀爬状态。
- 向上爬到一个平台之后，触发自动攀爬上平台
  - 做眼部射线，向前并向下
- 在边缘向下进入攀爬状态，此时的检测射线是身前的两条
- 然后开始做Control rig，把手脚贴到地面上
- 其次是做Vault Or Mantle检测，身前若干条线。使用MotionWarping，并添加Notify State使角色对齐到相应的位置
  - ![image-20250812234314689](README.assets/image-20250812234314689.png)

- 其次是做输入上下文切换，进入攀爬状态时切换攀爬上下文，进入走路状态时就进入走路上下文
  - 在这里使用委托：进入攀爬的委托，和出攀爬的委托。
  - 使用两个委托来实现输入上下文的切换

- (暂时没做)做HopUp 和 HopDown，需要使用chorded Action来实现。
  - 需要获取上一个输入的向量，来判断和世界上向量的夹角，从而判断是向哪边Hop

# 主要优化项

- [x] 手脚Lock
  - 在动画系列中添加手脚总共四条曲线
  - 在动画蓝图中判断上升沿和下降沿，并传递给Control Rig
  - 上升沿和下降沿需要是脉冲形式，因为锁住/释放时，只需要执行一次刷新目标点和插值强度的操作
  - 但是手部解放的时候，需要一直设置手目标位置为动画蓝图输入位置，即不额外改变位置
  - Full Body IK是一直持续的
  - Idle的IK位置和刚一进入爬行动画的手的设置：刚从idle出来的状态不锁只做贴墙面处理，正式进入循环之后再锁。
  - 弹簧插值，让手过渡丝滑
  - 匹配动画速度和运动速度
  - Control rig只是为了锁手/脚和贴墙用的，放在蒙太奇插槽之后
  - 射线未检测到位置时，用骨头的原位置
- [x] 向上爬到边界时，应该判断这个边界是不是水平的，判断当前攀爬的平面的法线和世界上方向夹角
- [x] 八向移动
  - 点积算角度，叉乘算左右
- [x] 下落中重新攀爬
- [x] Vault的Motion Warping设置
- [x] IK手脚，除了要锁定位置，还要锁定旋转，使其朝向Hit点的法线反方向
- [x] 不同的条件下判断应该Vault还是mantle
  - 判断之后的射线高度
- [ ] （暂时不做）墙上跳跃
- [ ] （暂时不做）从边缘爬下时，应该设置notify state提前进入攀爬状态，并且需要Motion Warping对齐爬下的位置

# 知识点杂项

### TObjectPtr

- 有如下特点：懒加载，性能无影响，打包之后会被替换为原始指针。
- ![image-20250807043347930](README.assets/image-20250807043347930.png)

### UE5—左手定则

### 爬墙的贴墙处理

- 实际上并没有什么方便操作
- 就是先让角色每一帧都贴住墙
- 同时让通过叉乘，将本来往前后走改为上下走。

### Control Rig的 Forward Solve 和 Backward Solve

- ![image-20250812004142280](README.assets/image-20250812004142280.png)
